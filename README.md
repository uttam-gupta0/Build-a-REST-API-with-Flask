# Build-a-REST-API-with-Flask
A simple REST API built with Flask to manage user data. Supports CRUD operations (GET, POST, PUT, DELETE) using in-memory storage. Tested with Postman/VS Code REST Client. Demonstrates API fundamentals, HTTP methods, and JSON handling for beginners.
